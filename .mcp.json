{
  "mcpServers": {
    "shadcn-studio": {
      "command": "npx",
      "args": ["-y", "shadcn-studio-mcp@latest"],
      "env": {
        "EMAIL": "${SHADCN_STUDIO_EMAIL:-}",
        "API_KEY": "${SHADCN_STUDIO_API_KEY:-}"
      }
    }
  }
}
