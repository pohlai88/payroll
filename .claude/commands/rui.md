---
description: Refine an existing shadcn block, component, or page
argument-hint: [file or component] [what to change]
---

Call the shadcn-studio MCP tool `get-refine-instructions` FIRST, then follow the returned
instructions to refine: $ARGUMENTS

Edit the existing files in place — do not scaffold a replacement component unless the
instructions require it. Respect this project's `components.json` conventions.
