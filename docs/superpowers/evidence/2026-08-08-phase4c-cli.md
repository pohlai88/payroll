# Phase 4C CLI / Studio evidence

## Pinned CLI

- Resolved from `npm view shadcn version` at 2026-08-08T13:48:25Z: `PINNED_VERSION=4.16.2`
- Verify command: `npx shadcn@4.16.2 --version` → `4.16.2`

## Studio selection

- MCP: `user-shadcn-studio-mcp` required initial `mcp_auth` (first call returned "Not connected"); subsequent tools succeeded.
- Theme/preset: **none — tokens only via Straits** (Straits doctrine owns `:root` / `.dark` / `--doc-*`; no Studio `install-theme` in this slice).
- Components (minimum primitives for 4A/4B surfaces): `button`, `input`, `label`, `card`, `alert`, `table`, `badge`
- Studio MCP confirmation (`get-component-meta-content`):
  - `button` — catalog present; base primitive `button` referenced as `registryDependency` on variants (e.g. `button-01`).
  - `input` — catalog present; base primitive `input` referenced on variants (e.g. `input-01`).
  - `label` — no Studio variant catalog entry ("Component not found"); still required — referenced as `registryDependency` on input/card variants and listed in plan/spec.
  - `card` — catalog present; base primitive `card` referenced on variants (e.g. `card-01`).
  - `alert` — catalog present; base primitive `alert` referenced on variants (e.g. `alert-01`).
  - `table` — catalog present; base primitive `table` referenced on variants (e.g. `table-01`).
  - `badge` — catalog present; base primitive `badge` referenced on variants (e.g. `badge-01`).
- Optional peers (`separator`, `sonner`): **skipped** — not mandated by theme install (no theme) or shipped surfaces.
- Rejected / deferred: sidebar, dashboard, `/cui` blocks (primitives only; no product chrome).

## Commands to execute later

```text
npx shadcn@4.16.2 init ...
npx shadcn@4.16.2 add button input label card alert table badge
```

Task 2 notes: init for existing Vite SPA; style `base-nova` if Studio/CLI offers it; align aliases with `@/components`, `@/components/ui`, `@/lib/utils`, `@/hooks`.

## Task 2 execution (2026-08-08)

- Command: `npx shadcn@4.16.2 init -y -t vite -b base -p nova --no-monorepo`
- Resolved style in `components.json`: `base-nova` (via `-b base -p nova`; preset name `base-nova` invalid alone)
- Files touched (committed):
  - `components.json` — style `base-nova`, css `src/web/styles.css`, aliases match spec
  - `vite.config.ts` — `@tailwindcss/vite` plugin
  - `package.json` / `package-lock.json` — `tailwindcss`, `@tailwindcss/vite`, shadcn peer deps
  - `src/lib/utils.ts` — `cn()` helper
  - `src/web/styles.css` — `@import "tailwindcss"` + shadcn/tw-animate/geist imports + CSS vars
- Init also created (not in Task 2 commit scope): `src/components/ui/button.tsx`
- No sidebar shell, Next.js files, or `app.tsx`/auth/api rewrites
- Verify: `npm run typecheck` ✓, `npm run build` ✓

## Task 4: Install Studio-selected primitives (pinned CLI)

Status: COMPLETED

**Inspect notes:**
- Dry-run confirmed 7 components: button (skip, identical), input, label, card, alert, table, badge (6 new)
- No unwanted sidebar, nav chrome, or unrelated components
- Scope exactly matches expectation

**Execution summary:**
- Used pinned CLI shadcn@4.16.2 successfully
- Created 6 new components, skipped existing identical button.tsx
- Added success/warning/info variants to Badge and Alert using Straits status tokens
- Straits tokens preserved (tests/domain/straits-tokens.test.ts passed)
- Typecheck and build successful
- Commit fb7df3b includes all 7 components
